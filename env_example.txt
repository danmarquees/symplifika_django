# Django Settings
SECRET_KEY=django-insecure-your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,testserver

# PostgreSQL Database Configuration
# Descomente e configure para usar PostgreSQL ao invés de SQLite
# DATABASE_URL=postgresql://symplifika_user:symplifika_pass123@localhost:5432/symplifika_db

# CORS Settings (para desenvolvimento)
CORS_ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000,http://localhost:3000,http://127.0.0.1:3000
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Google Gemini API (obrigatório para funcionalidades de IA)
GEMINI_API_KEY=your_gemini_api_key_here

# Stripe Payment Configuration (obrigatório para funcionalidades de pagamento)
STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
STRIPE_SECRET_KEY=sk_test_your_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here
STRIPE_RETURN_URL=http://localhost:3000/account

# Configurações de Email (opcional)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your_email@gmail.com
# EMAIL_HOST_PASSWORD=your_app_password
# DEFAULT_FROM_EMAIL=your_email@gmail.com

# Render.com Production Settings (automaticamente configurado no deploy)
# RENDER_EXTERNAL_HOSTNAME=your-app-name.onrender.com

# Configurações Adicionais
# DJANGO_SETTINGS_MODULE=symplifika.settings
# PORT=8000

# Instruções:
# 1. Copie este arquivo para .env: cp env_example.txt .env
# 2. Configure sua GEMINI_API_KEY (obtenha em: https://makersuite.google.com/app/apikey)
# 3. Configure suas chaves do Stripe (obtenha em: https://dashboard.stripe.com/apikeys)
# 4. Para PostgreSQL local: descomente e configure DATABASE_URL
# 5. Para SQLite (padrão): deixe DATABASE_URL comentado
